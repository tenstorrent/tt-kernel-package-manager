# tt-kernel-package-manager
This is a wrapper around huggingface that allows users to upload and distribute versioned kernels for Tenstorrent kernels and configs so that users can pull model, pull config and run the models in working pre-tuned configurations.
